# Server port - The port on which the application will run (default: 3000)
PORT=3000

# Companion UI Web Server Port
WEB_PORT=3001

# App package name - Your application's unique identifier.  Make sure it exactly matches the package name in the MentraOS Developer Console
PACKAGE_NAME=com.yourname.memoryapp

# API key - Your MentraOS developer API key from the MentraOS Developer Console (never commit the key to source control)
MENTRAOS_API_KEY=your_mentraos_api_key_here

# AssemblyAI API key for speaker diarization and transcription
ASSEMBLYAI_API_KEY=your_assemblyai_api_key_here

# OpenAI API key for GPT-4o-mini (name extraction and summarization)
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model Selection
# Options: gpt-4o-mini (default, cheapest), gpt-4o (most capable), gpt-4-turbo, gpt-3.5-turbo
# Recommended: gpt-4o-mini for best balance of cost/performance
OPENAI_MODEL=gpt-4o-mini

# Supabase Configuration (Production Storage)
# Get these from your Supabase project dashboard: Settings > API
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Cookie secret for MentraOS authentication (change in production)
COOKIE_SECRET=change-this-secret-in-production